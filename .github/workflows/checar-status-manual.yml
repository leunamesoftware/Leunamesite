name: Checar status de uma chave (publico)

on:
  workflow_dispatch:

jobs:
  checar:
    runs-on: ubuntu-latest
    steps:
      - name: Status da chave do Emanuel (ConstruGestao)
        run: curl -sS "https://api.leunamesoftware.com/licencas/verificar?chave=LEU-4Z1H-RL7U-B1FD"
