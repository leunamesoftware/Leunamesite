name: Verificar planos publicos do LeuCloud (pontual)

on:
  workflow_dispatch:

jobs:
  verificar:
    runs-on: ubuntu-latest
    steps:
      - name: Consultar /billing/planos (rota publica, sem credenciais)
        run: curl -sS --fail "https://leucloud-api.emanuelantunes2024.workers.dev/billing/planos"
